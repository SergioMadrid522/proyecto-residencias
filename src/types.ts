export type OptionMenu = {
  label: string;
  link: string;
  icon: string;
  viewbox: string;
};

export type StatsOverview = {
  icon: string;
  iconViewbox: string;
  title: string;
  stats: number;
};
